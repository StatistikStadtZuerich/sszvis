---
"sszvis": patch
---

type `sszvis.legendColorLinear().labelFormat()` against the numbers it is handed when `labelText` is unset, and give `sszvis.legendRadius().tickFormat()` d3's index, nodes and `this`
