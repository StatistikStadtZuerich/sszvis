---
---

Dropping type assertions the compiler already discarded, plus lint and formatter
configuration. No runtime behaviour and no public types change, so
`packages/sszvis` gets no release from this branch.
