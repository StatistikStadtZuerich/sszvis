---
"sszvis": patch
---

position `sszvis.sunburst()` tooltip anchors from the destination angles instead of the angles currently on screen, so they no longer trail a render behind the arcs after an update
