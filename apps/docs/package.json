{
  "name": "@sszvis/docs",
  "version": "0.0.0",
  "private": true,
  "description": "11ty documentation site with live examples for sszvis",
  "license": "BSD-3-Clause",
  "dependencies": {
    "@sszvis/geodata": "workspace:*",
    "sszvis": "workspace:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@babel/core": "^7.28.4",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && eleventy",
    "dev": "eleventy --serve --port 8000",
    "start": "eleventy --serve --port 8000",
    "serve": "eleventy --serve --port 8000"
  }
}
